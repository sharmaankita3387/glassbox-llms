NOTE THAT IN ORDER TO RUN THIS VIA A CONFIG.JSON, YOU MUST SUPPLY ONE OF THE VALUES SPECIFIED IN `DATASET_MAP` IN `evaluator.py` as `"name"` in the dataset field of the config json!!!

The actual dataset specified in the config will do nothing: I am just using the premade .json files in /questions since I couldn't get hf loading to work in the necessary timeframe.
